// -----------------------------------------------------------------------------
// Supabase client — placeholder for Lesson 7 (Backend).
//
// The frontend currently runs on mock data (see src/data/*). When you wire up
// the backend:
//   1) npm install @supabase/supabase-js
//   2) create a .env file with:
//        VITE_SUPABASE_URL=your-project-url
//        VITE_SUPABASE_ANON_KEY=your-anon-key
//   3) replace the export below with the real client:
//
//        import { createClient } from '@supabase/supabase-js'
//        export const supabase = createClient(
//          import.meta.env.VITE_SUPABASE_URL,
//          import.meta.env.VITE_SUPABASE_ANON_KEY,
//        )
//
// Keeping it null for now so nothing breaks before the backend exists.
// -----------------------------------------------------------------------------

export const supabase = null
